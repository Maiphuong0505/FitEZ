<!DOCTYPE html>
<html>
  <head>
    <title>FitEz</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/css/tom-select.css">
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/tom-select/dist/css/tom-select.bootstrap5.min.css" %>
    <%= javascript_importmap_tags %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>
  <body
    data-controller="back-to-top"
    data-back-to-top-target="button"
    data-action="back-to-top#scrollFunction">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    <%= yield %>
    <% unless controller_name == "pages" && action_name == "home" %>
      <button id="back-to-top" class="btn btn-primary rounded-circle shadow" data-action="back-to-top#scrollToTop"> Back to top </button>
    <% end %>
    <footer>
      <%= render "shared/footer_navbar" %>
    </footer>
  </body>
</html>
